# Given t1 = (1, 2, 3) and t2 = (4, 5, 6), concatenate them into a single tuple.

# Given tuples
t1 = (1, 2, 3)
t2 = (4, 5, 6)

# Concatenating tuples
result = t1 + t2

# Printing the result
print("Concatenated Tuple:", result)